<div dir="rtl" style="text-align: right;">

# خطة الاختبارات الشاملة — Amlaak Video Library Test Plan

تغطي هذه الخطة كافة مستويات التحقق الهندسي المطلوبة لضمان استقرار وموثوقية المنظومة قبل التشغيل الفعلي.

---

## 🧪 مصفوفة الاختبارات (Test Matrix)

### 1. اختبارات الوحدة المؤتمتة (Automated Unit Tests — 18 اختباراً)
- **استخراج معرّف Drive File ID:** فحص روابط `/file/d/`، روابط `?id=`، الروابط القياسية، والمعرفات الخام.
- **تنقيح أسماء الملفات (Sanitization):** إزالة الرموز الممنوعة في أنظمة التشغيل (`\ / : * ? " < > |`)، تنظيف المسافات الزائدة، وتوحيد الفواصل مع الحفاظ على شرطات التواريخ (`YYYY-MM-DD`).
- **قواعد التسمية (§12) لفيديوهات المشاريع:** مطابقة الصيغة القياسية بدقة مع التأكد من عدم تضمين تصنيف الأعمال (`Work Category`) في اسم الملف الفيزيائي وفق القرار A.
- **قواعد التسمية للمحتوى التسويقي:** إخفاء نوع الفراغ تلقائياً لمحتوى `Educational`، وتضمينه عند توفره لمحتوى `Demonstration`.
- **التسلسل الرقمي وتوليد المعرفات:** زيادة رقم الفيديو (`0001` إلى `0002`)، أرقام النسخ (`V01` إلى `V02`)، وأكواد الوحدات (`U-0001`).
- **نشر تعديلات الوحدة وحساب إعادة التسمية المجمعة (القرار C):** التحقق من اكتشاف جميع الملفات التي تتغير أسماؤها عند تعديل سجل الوحدة.
- **صحة معادلات لوحة المؤشرات (BI Metrics §19):** حساب الفيديوهات المنطقية كفيديوهات فريدة، دون مضاعفتها بتعدد النسخ الفيزيائية.
- **التعامل مع حقول المدة والاتجاه (§7.2):** قبول السجلات الفارغة دون أخطاء، واستنتاج الاتجاه فقط عند توفر الأبعاد كاملة.

---

### 2. اختبارات التكامل والمحاكاة (Integration Simulation Tests — 5 اختبارات)
- **متطلب صلاحية الـ Editor المسبق (§6 و §13):** التأكد من إظهار رسالة الخطأ المحددة عند محاولة إضافة ملف غير مشارك بصلاحية Editor مع حساب التطبيق.
- **حفظ واستقرار النسخ السابقة (§14):** التحقق من قلب النسخة السابقة إلى `No` مع إبقاء ملف Drive دون مسح أو استبدال.
- **تتبع نسخ مخططات التصميم (القرار B):** التحقق من تسلسل نسخ الـ PDF لكل وحدة (`DOC-U0001-V01`, `V02`).
- **صمود إعادة التسمية المجمعة أمام الأعطال الجزئية (القرار C):** محاكاة نجاح إعادة تسمية ملفات وفشل ملف بسبب حصة Google أو صلاحية، والتأكد من إتمام المعالجة ورفع تقرير دقيق بالملفات المتبقية دون توقف المنظومة.

---

### 3. اختبارات واجهة المستخدم والتفاعل (UI / UX & Accessibility Tests)
- الاتجاه العام للواجهة: RTL كامل للعربية، و LTR للأكواد والمعرفات وأسماء الملفات والتواريخ.
- استجابة التصميم: التوافق الكامل مع شاشات الكمبيوتر واللاب توب والتابلت والموبايل.
- حالات التحميل والتنبيهات: ظهور مؤشرات المعالجة ورسائل الـ Toast الملونة والتأكيدات الصريحة قبل العمليات المؤثرة.

</div>
