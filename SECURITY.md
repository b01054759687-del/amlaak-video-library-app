<div dir="rtl" style="text-align: right;">

# سياسة الأمان والصلاحيات — Amlaak Video Library Security Policy

يوثق هذا المستند النموذج الأمني الصارم المطبق في المنظومة لضمان حماية أصول وبيانات شركة أملاك ديزاين.

---

## 🛡️ مبادئ الأمان الأساسية (Core Security Principles)

1. **التحقق الخادمي الصارم (Server-Side Enforcement):**
   - لا يتم الاعتماد نهائياً على إخفاء الأزرار في واجهة المستخدم (`Client-side`).
   - كل استدعاء خادمي (`Server API Gateway` في `Code.gs`) يمر أولاً عبر دالة `Auth.requireAuth()` للتحقق من هوية الحساب وصلاحياته قبل قراءة أو كتابة أي بايت في Google Sheets أو Google Drive.

2. **الإغلاق التلقائي عند الشك (Fail-Closed Architecture):**
   - إذا تعذر التحقق من بريد المستخدم المسجل به في Google، أو كان حسابه غير مسجل في جدول `Authorised Users`، يتم رفض الطلب فوراً وتوليد كود الخطأ `UNAUTHORIZED` وتوثيق المحاولة في `Audit Log`.

3. **حساب التنفيذ والصلاحيات المسبقة على Drive:**
   - يعمل تطبيق الويب بنموذج `Execute as: Me` (حساب المشرف الناشر للتطبيق).
   - يتطلب ذلك أن يمتلك حساب المشرف صلاحية **Editor** على الملفات المراد فهرستها.
   - تبقى مجلدات `Amlaak Video Library` مقيدة (`Restricted`) ولا يتم فتح صلاحياتها للعامة إطلاقاً.

4. **حظر تخزين الأسرار ومفاتيح الـ OAuth على GitHub:**
   - ملف `.gitignore` يستبعد تماماً أي ملفات سرية (`.clasprc.json`, `.clasp.json`, `.env`, `credentials.json`, وتصديرات البيانات الحقيقية).
   - يتم تخزين معرّفات الموارد العامة فقط داخل `ScriptProperties` الآمنة على خوادم Google Cloud.

5. **سجل التدقيق والأمان المباشر (Audit Logging):**
   - توثيق كل عملية إضافة فيديو، إضافة نسخة، رفع مخطط تصميم، تعديل ميتاداتا، أو إعادة تسمية مجمعة في شيت `Audit Log`.
   - تسجيل إيميل المستخدم، التوقيت بدقة الثواني، المعرّف، النتيجة (`SUCCESS` أو `FAILURE`)، وكود الخطأ الآمن بدون تسريب أي Stack Trace حساس للعميل.

</div>
